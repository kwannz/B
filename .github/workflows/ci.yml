name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  skip-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Skip CI
        run: |
          echo "CI checks are disabled"
          exit 0
