pump_fun:
  api_key: "${PUMP_FUN_API_KEY}"  # Set via environment variable

solana:
  rpc_key: "${SOLANA_RPC_KEY}"

database:
  mongodb:
    password: "${MONGODB_PASSWORD}"
  postgresql:
    password: "${POSTGRESQL_PASSWORD}"
  redis:
    password: "${REDIS_PASSWORD}"
